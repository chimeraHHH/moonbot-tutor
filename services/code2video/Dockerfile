# Placeholder Dockerfile for the FastAPI code2video (Deep Solve) service.
# The real service will be ported in from real-time-tutor/services/code2video.
FROM python:3.12-slim
WORKDIR /app
RUN echo "code2video service not yet ported" && exit 0
EXPOSE 8010
CMD ["echo", "code2video placeholder"]
